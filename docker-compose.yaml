version: "3.8"

services:
  ca-web:
    build: .
    volumes:
      - ./site/:/var/www/html
    ports:
      - "5000:80"
    environment:
      DB_HOST: rgl-converterapp-db.c8h0syswkbi9.us-east-1.rds.amazonaws.com
      DB_NAME: converterApp
      DB_USER: admin
      DB_PASS: your_db_password